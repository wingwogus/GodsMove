# 영농일지 코칭 피드백 조회

API Path: /api/v1/farming-records/{recordId}/coaching-feedback
Index: RECORD-008
Method: GET
Request: None
Response: CoachingFeedbackResponse

## Response Fields

- `feedbackId`: uuid
- `recordId`: uuid
- `status`: `PENDING | READY | FAILED | STALE`
- `sourceRevision`: number
- `inputPrepared`: boolean. 실제 엔티티 기반 입력 스냅샷 준비 여부
- `failureCode`: string, nullable
- `createdAt`, `updatedAt`: datetime

## Rule

인증 회원이 소유하고 삭제되지 않은 영농일지만 조회한다. 현재 원본 revision의
피드백을 우선 반환하고, 현재 revision 행이 없을 때만 가장 최신 `STALE` 행을
반환한다.

2단계에서는 `inputSnapshot`, LLM 결과, 인용, 모델·감사 정보를 응답에 노출하지
않는다. 정상적으로 준비된 행도 LLM 생성 전까지는 `PENDING` 상태다.
