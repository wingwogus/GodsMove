# ChamChamCham Frontend Agent Guide

The frontend workspace is not scaffolded yet.

When frontend code is added, keep frontend-specific architecture, commands,
test strategy, styling rules, and framework conventions in this file instead
of the repository root guide.

Follow YAGNI: do not add code, dependencies, or abstractions before a concrete need exists.

Common project rules still come from the root [AGENTS.md](../AGENTS.md).
