# 검색(Search) Figma Captures

이 폴더는 TalkToFigma MCP로 수집한 검색 화면의 Figma 캡처와 구현 준비 메모를
저장한다. 사용자가 화면을 하나씩 순서대로 전달하는 방식으로 진행 중이며,
총 7개 상태(검색 진행 시 3개 + 검색 결과 화면 4개)를 수집할 예정이다. 모두
수집한 뒤 기존 SwiftUI 화면, 디자인 시스템, 관련 Business Rule, 배포 Swagger를
함께 대조해 구현 계획을 확정한다.

## MCP Connection

- TalkToFigma channel: `chamchamcham`
- Figma에서 화면 또는 상태 프레임 하나만 선택한다.
- 캡처 순서: `get_selection` → `read_my_design` → `scan_text_nodes` →
  `export_node_as_image`
- 문서 전체 조회는 사용하지 않는다.
- PNG 참고: Claude Code에서 `export_node_as_image`는 결과를 인라인 이미지로만
  반환하고 디스크 파일로 저장하지 않는다. 구조·텍스트·색상은 `read_my_design`
  값으로 정밀 기록하고, PNG 파일이 필요하면 Figma에서 수동 Export(2x)한다.
- 연결 절차(socket relay, MCP 등록, 플러그인 connect) 전체는
  [record/HANDOFF.md](../record/HANDOFF.md) Part 1과 동일 — 이 폴더에서는 반복하지 않는다.

## Captures

- [검색 / default](2026-07-16-search-default.md) — `1547:23510`, 검색어 미입력 +
  최근 검색어 0건(헤더 + "전체 삭제" 버튼만 노출). `AppSearchBar`와 완전 일치.
- [검색 / default - 최근 검색어 존재 시](2026-07-16-search-history-exists.md) —
  `1547:23476`, 최근 검색어 6개(2행×3열 wrap chip). `AppChip`과 거의 일치하나
  border color 불일치(`#e0e0e0` vs `AppChip` 기본 `#f3f3f3`) 확인 필요.
- [검색 / 검색어 입력 중](2026-07-16-search-typing.md) — `1547:23494`, 입력값 있는
  검색바(clear 아이콘) + "동일 검색어" 1개 + "연관 검색어" 9개 제안 리스트. 신규 DS
  컴포넌트 불필요(Text+AppDivider 화면 내부 처리), `AppDivider` 기본색도 `#f3f3f3` vs
  `#e0e0e0` 불일치.
- [검색 결과 / default](2026-07-16-search-result-default.md) — `1547:23518`, 전체/나의
  일지/정책 정보/게시글 4탭 + 카테고리별 최대 3개 미리보기. `AppListItem`의
  `.large`/`.xlarge`/`.medium` variant와 `AppTabBar`가 색상·치수까지 정확히 일치 —
  신규 컴포넌트 전혀 불필요. 검색 대상 3종(나의 일지/정책 정보/게시글) 확정.
- [검색 결과 / 나의 일지 탭](2026-07-16-search-result-record-tab.md) — `1547:23557`,
  `AppListItem.large` 재사용. 새 "총 N개" 헤더 밴드 패턴, 단 헤더 개수·구분선 모두
  결함 있는 placeholder 상태(아래 이슈 참고).
- [검색 결과 / 정책 정보 탭](2026-07-16-search-result-policy-tab.md) — `1547:23573`,
  `AppListItem.xlarge` 재사용. 구분선 규칙은 정상, 헤더 개수만 잘못됨.
- [검색 결과 / 게시글 탭](2026-07-16-search-result-post-tab.md) — Figma node
  `1547:23587`(레이어 이름은 "정책 정보 탭"으로 오기, 실제 콘텐츠는 게시글).
  `AppListItem.medium` 재사용. **탭바 선택 상태가 "정책 정보"로 남아있어 콘텐츠와
  모순** — 구현 시 그대로 베끼면 안 됨.

## Capture Checklist — 7/7 완료

### 검색 진행 시 (3개) — 완료

1. **default** — 완료 (최근 검색어 0건)
2. **최근 검색어 있음 상태** — 완료 (6개 chip, wrap 그리드)
3. **검색어 입력 중 상태** — 완료 (동일/연관 검색어 제안 리스트)

### 검색 결과 화면 (4개) — 완료

4. **검색 결과 — 기본/성공 상태** — 완료 (전체 탭, 3섹션×3개 미리보기)
5. **검색 결과 — 나의 일지 탭** — 완료 (단일 카테고리 풀리스트, 구분선 결함 있음)
6. **검색 결과 — 정책 정보 탭** — 완료 (단일 카테고리 풀리스트, 정상)
7. **검색 결과 — 게시글 탭** — 완료 (레이어 이름 오기 + 탭바 선택 모순 발견)

## 구현 전 확인 필요한 Figma 결함 (사용자/디자이너 확인 대기)

세 탭 프레임(5~7번)을 상세 대조한 결과 "탭만 다른 동일 화면"이 아니라 몇 가지
Figma 작성 결함이 발견됨 — 구현 계획에 그대로 인코딩하지 말 것:

1. **헤더 "총 N개" 텍스트가 세 프레임 모두 "총 12개"로 동일** — 정책 정보(정답 7)와
   게시글(정답 5) 탭에서는 명백히 틀림. 실제 바인딩 로직 필요, Figma 값은 placeholder.
2. **렌더링된 아이템 수가 실제 총 개수와 불일치**하는 탭이 2개(나의 일지: 6개 표시/
   총 12개, 정책 정보: 4개 표시/총 7개) — 페이지네이션 여부 등 백엔드팀 확인 필요.
3. **나의 일지 탭만 구분선 규칙이 깨짐**(마지막 1개가 아니라 4개 연속 구분선 없음) —
   Figma 복제 실수로 판단, "마지막 아이템만 구분선 생략" 규칙 유지 권장.
4. **게시글 탭 프레임의 탭바 선택 상태가 "정책 정보"로 남아 콘텐츠와 모순** — 정책
   정보 탭 프레임을 복제해 콘텐츠만 게시글로 바꾸면서 탭바를 안 고친 것으로 추정.

Figma에 없는 런타임 상태는 기존 디자인 시스템 패턴을 사용하되 구현 계획에
명시한다.

Claude 또는 다른 AI 세션에서 캡처와 구현 작업을 이어갈 때는
[HANDOFF.md](HANDOFF.md)를 인수인계 문서로 사용한다.
